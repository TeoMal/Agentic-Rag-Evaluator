# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Secrets -- real values live only in .env (template: .env.example)
.env

# Tool caches
.pytest_cache/
.ruff_cache/

# Per-run deploy logs written by scripts/deploy.py
logs/

# MCP register of recorded assessments when MCP_STATE_DIR=./state (local runs)
state/
